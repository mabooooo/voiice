# Capabilities

这里用于收纳 `bridge` 的本地能力部署脚本与适配层。

设计约束：

- 每个能力单独放在一个子目录下，便于后续继续接入 OCR、视觉、代理等能力。
- 可提交到仓库的部分只保留脚本、说明和适配代码。
- 大模型权重、虚拟环境、上游仓库克隆产物等本地部署文件统一落在各能力目录自己的 `.local/` 下，并通过 `.gitignore` 忽略。

当前已接入能力：

- `omniparser`：本地桌面截图解析能力，供开发者模式测试。
- `sensevoice`：本地语音识别能力，供设置页开启并行转写。
